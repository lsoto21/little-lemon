export const Header = () => {
    return (
        <div style={styles.header}>Header</div>
    )
}

const styles = {
    header: {
        display: "flex",
        backgroundColor: "#495E57",
        color: "white",
    }
}